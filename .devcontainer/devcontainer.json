{
	"name": "melanopsin-paper (Python 3.10 + R)",
	"build": {
		"context": "..",
		"dockerfile": "Dockerfile"
	},
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-toolsai.jupyter",
				"reditorsupport.r",
				"rdebugger.r-debugger"
			]
		}
	},
	"postCreateCommand": "if [ -f requirements.txt ]; then pip install --user -r requirements.txt; fi"
}
